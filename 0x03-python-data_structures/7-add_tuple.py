#!/usr/bin/python
def add_tuple(tuple_a=(), tuple_b=()):
    for nb in tuple_a:
        if not isinstance(nb, int):
            return
    for nb in tuple_b:
        if not isinstance(nb, int):
            return
    somme = tuple(0,0)
    for nb in somme:
        for i in range(2):
            for
